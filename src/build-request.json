{
  "kind": "build_request",
  "title": "Fix missing media previews in preview/live modes, restore audio playback, and clarify Media button label",
  "priority": "high",
  "requirements": [
    {
      "id": "REQ-42",
      "text": "Ensure uploaded/published media is visible in both the app's preview experience and the live/production experience: Hero background, all Image Memory slots, all Video Memory slots, and the background song must render using the current MediaStore state and must correctly fall back to backend-published media when local IndexedDB media is absent.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-6"
        ],
        "quotes": [
          "In live section in previous section I am not visible to see the uploaded images in Hero section in image section and in video section"
        ]
      },
      "acceptanceCriteria": [
        "On a fresh load with an empty browser storage (no IndexedDB/localStorage media), the app fetches backend published media and displays: hero background (if published), any published image slots, any published video slots, and the published song (if present).",
        "In the live/production deployment, the Hero section background and the Image/Video Memory sections visibly render the uploaded/published media without requiring a manual refresh.",
        "If a slot is cleared in published media, the corresponding UI slot falls back to the existing placeholder media for that slot (no broken/pink/blank placeholders)."
      ]
    },
    {
      "id": "REQ-43",
      "text": "Add an explicit, user-friendly pre-publish preview inside the Media Manager that lets the user confirm what they uploaded for Hero, Images, Videos, and Song before publishing, and make sure the preview uses the staged draft values (not only the currently published/live values).",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-6"
        ],
        "quotes": [
          "please show the preview of uploaded image in the hero section image section and video section so I can go for it before publishing"
        ]
      },
      "acceptanceCriteria": [
        "Media Manager includes a clear section labeled in English (e.g., \"Preview\") that shows: the current draft Hero background, a grid preview of draft images in the current draft order with transforms applied, a grid preview of draft videos, and an audio preview for the draft song.",
        "The preview reflects staged draft uploads/replacements/clears immediately after the user selects files, without requiring Submit.",
        "When a slot is staged as 'clear' in the draft, the preview shows the placeholder/fallback for that slot (and does not continue showing an old image/video)."
      ]
    },
    {
      "id": "REQ-44",
      "text": "Fix background audio playback so the user can reliably hear the song in both preview and live/production modes, including when the user uploads a custom song; ensure playback works across common mobile browsers where autoplay may be blocked.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-6"
        ],
        "quotes": [
          "I am not listening audio also in preview section and in live section"
        ]
      },
      "acceptanceCriteria": [
        "If autoplay is blocked, the UI provides an English call-to-action that clearly instructs the user to tap to start audio (e.g., the existing MusicPlayer play button must successfully start audible playback).",
        "When the user uploads/replaces the background song and submits, the MusicPlayer updates to the new song and the user can play/pause it normally.",
        "Audio playback works for supported upload types already accepted by the UI (.mp3 and .wav). If the file is not MP3, the audio element must still attempt playback (do not hardcode only audio/mpeg sources)."
      ]
    },
    {
      "id": "REQ-45",
      "text": "Update the Hero section Media entry-point button to remove the confusing \"Edit\" label and show a clear, consistent label in English on all screen sizes (keep the icon).",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-6"
        ],
        "quotes": [
          "remove the edit button of media role button so please show it clearly"
        ]
      },
      "acceptanceCriteria": [
        "On mobile and desktop, the Hero section top-right button label reads \"Media\" (not \"Edit\").",
        "The button retains an accessible English aria-label describing what it opens (media management for hero/images/videos/song)."
      ]
    },
    {
      "id": "REQ-46",
      "text": "Ensure 'clear' actions are easy to understand and reliably remove media references so the UI falls back to placeholders (Hero, individual image slots, individual video slots, and song) in both preview and live views after Submit.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-6"
        ],
        "quotes": [
          "fix everything I can properly clear reference"
        ]
      },
      "acceptanceCriteria": [
        "Clearing a Hero/Image/Video/Song in the Media Manager draft updates the Media Manager preview immediately to show the placeholder/default for that media.",
        "After clicking Submit, cleared items are removed from the live app view immediately (Hero/Image grid/Video grid/Song), without requiring a refresh.",
        "After a full page reload, cleared items remain cleared (i.e., they do not reappear from stale local URLs)."
      ]
    }
  ],
  "constraints": [
    "All user-facing text must be in English.",
    "Do not edit files under frontend/src/components/ui (shadcn/ui) or other immutable paths listed in SYSTEM_CONTEXT.",
    "Backend must remain a single-actor Motoko canister; only add migration changes if existing stable state shape must change."
  ],
  "nonGoals": [
    "Adding new authentication methods beyond Internet Identity.",
    "Adding third-party storage/CDN for media.",
    "Implementing real-time syncing or WebSocket-based updates."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}